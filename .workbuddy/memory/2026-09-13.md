# 2026-09-13

## 接入 Linear MCP 到 WorkBuddy

- 背景：用户希望 WorkBuddy 能读写 Linear 项目 `ModernTodoList`（workspace `inhandy`）。
- 结论：推荐市场无 Linear 连接器，改用自定义 MCP 走 Linear 官方远程端点。
- 新建 `C:\Users\Lamires\.workbuddy\mcp.json`，HTTP 模式指向 `https://mcp.linear.app/mcp`，`Authorization: Bearer <Linear 个人 API Key>`（明文存储，密钥不记录在此）。
- 验证结果（均为只读）：
  - API 密钥有效，账户为 Lamires Asu；
  - MCP 端点接受 `Bearer` 前缀；服务端为**无状态**模式（initialize 不返回 `Mcp-Session-Id`）；
  - 响应为 SSE（`text/event-stream`），需按 `data:` 帧解析；
  - 共 73 个工具，含写入类 `save_status_update` / `save_issue` / `save_comment`。
- 目标项目标识：`ModernTodoList`，slugId `10ffd182261c`，project id `72e576a7-e894-4e22-b52c-5d47d43a5912`，state `backlog`。
- 「进展」= Linear Project Update，写操作走 `save_status_update`（`projectId` / `body` / `health`: onTrack|atRisk|offTrack）。
- 待办：用户在「连接器管理 → 自定义连接器」点击「信任」，连接器才会真正挂载为可用工具。

## 备注

- Linear GraphQL 直连时个人 API Key **不需要** `Bearer` 前缀，但 MCP 端点**需要**——两者不一致，排错时注意区分。
